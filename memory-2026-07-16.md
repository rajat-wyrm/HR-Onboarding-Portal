# HR Portal — Session Memory (2026-07-16)

## Objective
- Mega upgrade of HR onboarding portal: full server↔client wiring, robust, modern animated 3D UI, best compatibility. Manual deploy only (clasp rejected).

## Credentials / Config
- HR_USERID="admin", HR_PASSKEY="admin". verifyHRLogin returns boolean; client accepts `r===true || (r&&r.success===true)`.
- CONFIG.WEBAPP_URL: now auto-resolved at runtime via resolveWebAppUrl() (ScriptApp.getService().getUrl() cached in ScriptProperties). Placeholder no longer blocks validation-failed emails.

## Sheets
- Intern Data (25 cols), Project Details (Group Name,Email ID,WhatsApp Link,Domain Name), Program Details (Domain Name,Project Group Name).

## Robustness fixes applied this session (2026-07-16)
- Added header-key resolvers: _normKey, getCol, valAt, setCol. All fixed column-index reads/writes now go through actual sheet header (order-independent).
  - resolveGroupInfo, srTlRowHtml, fireOnboardingMails, sendTLMail, sendBulkEmails, updateInternRow dup-check, forceCodeGeneration all routed to header keys.
- fetchInternRecords now stores each record under BOTH normalized key (DomainName) and raw header (Domain Name). FIXES critical bug: client analytics used raw-header keys ('Domain Name','Current Status', etc.) which were always undefined -> dashboard silently empty.
- Client saveEdit validation fixed: now checks normalized keys f['EmailID']/f['DomainName'] (was f['Email ID']/f['Domain Name'] -> always undefined -> save blocked).
- saveAddIntern now sends normalized keys to addInternRecord (was raw keys -> server addInternRecord read normalized -> empty email/domain inserted). Validation fixed likewise.

## UI (prior session)
- Mega CSS upgrade: aurora mesh bg, glassmorphism, 3D stat cards/bars, glowing donut/gauge, pulsing feed, animated tabs/buttons, sticky blurred table, modal blur-in, reduced-motion gated. node --check OK.
- buildStats (9 animated cards + KPI strip) + buildAnalytics (3D bar, donut, 2 radial gauges, feed) from currentRecords.
- Add Intern flow + perf fixes (debounced filter, lazy tab render, groupCache, single project fetch).

## Verified syntax
- Code.gs OK, PdfParser.gs OK, Index.html JS OK (all via node --check).
- Server audit: all 24 client google.script.run funcs exist server-side. doGet returns Index.html; doPost JSON API switch on action.

## Blocked / Deploy notes
- Manual deploy only (clasp OAuth rejected). Must redeploy all 4 files: Code.gs, Index.html, PdfParser.gs, MailTemplates.gs.
- Advanced Drive API must be ENABLED (Editor > Resources > Advanced Google services > Drive API = ON) for PDF parsing.
- Live app stale until redeploy.

## Next
- Await user decision: single-page dashboard overview vs keep 3-tab layout.
- If no layout change: give e2e verification checklist + manual redeploy steps.

## Program/Project Details fixes (round 2, 2026-07-16)
CRITICAL:
- saveProjectEntry had ARG-SWAP bug: called .writeProjectSetting(rn, dm, gn, em, wa) but server sig is (rowNum, groupName, emailId, whatsappLink, domainName). Every project save corrupted data. FIXED to .writeProjectSetting(rn, gn, em, wa, dm).
- populateProjectDropdowns was CIRCULAR: built projDomain from currentProject's own domains -> empty on first load. FIXED to source from currentProgram + allDomains (authoritative).
- editProjectEntry: if project group not in dropdown options, select silently blanked. FIXED: append missing option before select.

Server consistency:
- saveRowModifications routed through setCol(header-key) instead of fixed indices.
- handleInternRegistration dup-check, updateInternRow, fetchProjectGroupsByDomain, resolveGroupInfo now match project domain via COMMA-SPLIT (multi-domain "Web, App" resolves to interns of either domain).

UI polish:
- rowEnter stagger animation; saved-flash on just-saved row.
- writeProgramSetting/writeProjectSetting return rowNumber; client flashSavedRow().

## Speed/perf upgrades (round 3, 2026-07-16)
Server (real, measurable):
- Added CacheService.getScriptCache() layer: _readCache/_writeCache/clearDataCache. fetchInternRecords now reads cache first (600s TTL) and writes on success. Login (getDashboardInit) and every refreshRecords now serve cached payload in ms instead of re-reading the whole sheet.
- clearDataCache() called on EVERY writer: saveRowModifications, forceCodeGeneration, fireOnboardingMails, writeProjectSetting, deleteInternRecord, updateInternRow, updateInternFullRecord, addInternRecord, writeProgramSetting, sendTLMail, sendBulkEmails. Invalidates records cache so UI always fresh after writes, near-instant on next load.
- getDashboardInit now derives domains from already-fetched Program Details (removes a redundant 3rd sheet read of Program sheet). One fewer Spreadsheet round-trip on every login.

Client (perceived speed):
- buildStats: _statsBuilt flag -> stat cards no longer re-run card-enter fade on every refresh (only on first load); count-up still plays. Snappier refresh.
- renderTableHead: _headSig guard -> skips rebuilding <thead> when headers unchanged, preserving open column-filter menus and avoiding needless DOM write on every refresh.

No downgrades: all writes still invalidate cache; program/project reads stay live (uncached, tiny); analytics still renders on dashboard.

## UX/UI fixes (round 4, 2026-07-16)
User requests:
- Edit modal: Current Status must list ALL 5 (Active, Pending Verification, Completed, Terminated, Resigned) and Validation Status must list Passed/Failed. VERIFIED buildEditForm already loops EDIT_STATUS_OPTIONS (5) + EDIT_VALIDATION_OPTIONS (Passed/Failed) and pre-selects current value — correct, no change needed.
- Project Details "add project group": replace clunky <select multiple> with CHIP MULTISELECT for domains.
  - HTML: projDomain now hidden input + #projDomainChips container + #projDomainInput (text, datalist #projDomainOptions) + #projDomainChipList.
  - JS: projAddDomain/projRemoveDomain/renderProjDomainChips. Type + Enter adds chip; datalist pick (onchange) adds; × removes. Chips animate (chipPop). populateProjectDropdowns now fills datalist (sorted) + group select.
  - saveProjectEntry reads hidden #projDomain (comma string). editProjectEntry renders chips from r.domainName. resetProjectForm clears chips.
  - CSS: .chip-multi / .chip / .chip-x / .chip-input added (focus glow, pop animation, red hover on remove).
- Registration enable/disable button: made INSTANT (optimistic UI). toggleRegistration now flips label + toast immediately, saves in background, reverts only on failure. No blocking spinner.

## Verified
- node --check OK on Code.gs + Index.html JS after round-4 edits.

## Deploy checklist (manual)
1. Paste updated Code.gs, Index.html, PdfParser.gs, MailTemplates.gs into editor.
2. Enable Advanced Drive API (Services).
3. Deploy > New deployment > Web app (Execute as Me, Access: Anyone).
4. Verify: add/edit Program Detail; add Project Group (multi-domain); edit project shows correct group+domains.
